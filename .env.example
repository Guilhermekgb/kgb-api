# Exemplo de variáveis de ambiente para desenvolvimento / produção
# NÃO comite um arquivo .env real com segredos.

# Porta do servidor (padrão 3333)
PORT=3333

# Cloudinary (opcional) — para armazenar fotosClientes
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Forçar modo de armazenamento (opcional: cloudinary | firebase | local)
STORAGE_MODE=cloudinary

# SQLite DB local
SQLITE_FILE=./data.db

# CORS: lista de origens permitidas (separadas por vírgula). Deixe vazio para permitir todas (dev).
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5500

# Firebase (opcional) — se usar Firebase Storage como fallback
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
FIREBASE_STORAGE_BUCKET=

# AWS S3 (opcional) — se quiser presign + S3
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET=
AWS_REGION=
# .env.example — copie para .env e ajuste
# Porta do servidor
PORT=3333

# Firebase (opcional)
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_CLIENT_EMAIL=your-firebase-client-email@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...REPLACE_WITH_PRIVATE_KEY...\n-----END PRIVATE KEY-----\n"
FIREBASE_STORAGE_BUCKET=your-firebase-bucket.appspot.com

# Se preferir uploads por token em vez de Firebase Auth, defina:
BACKUP_UPLOAD_TOKEN=change-me-to-a-secure-random-string

# Retenção dos backups em dias (padrão 30)
BACKUP_RETENTION_DAYS=30

# Habilite DISABLE_AUTH=1 apenas em desenvolvimento local
# DISABLE_AUTH=1

# Cloudinary (opcional)
CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# Força o uso do Cloudinary (opcional): setar STORAGE_MODE=cloudinary
STORAGE_MODE=cloudinary

# Nota: mantenha segredos fora do repositório; use variáveis de ambiente no host/CI.
