# .env.example — copie para .env e ajuste
# Porta do servidor
PORT=3333

# Firebase (opcional)
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_CLIENT_EMAIL=your-firebase-client-email@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...REPLACE_WITH_PRIVATE_KEY...\n-----END PRIVATE KEY-----\n"
FIREBASE_STORAGE_BUCKET=your-firebase-bucket.appspot.com

# Se preferir uploads por token em vez de Firebase Auth, defina:
BACKUP_UPLOAD_TOKEN=change-me-to-a-secure-random-string

# Retenção dos backups em dias (padrão 30)
BACKUP_RETENTION_DAYS=30

# Habilite DISABLE_AUTH=1 apenas em desenvolvimento local
# DISABLE_AUTH=1

# Cloudinary (opcional)
CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# Força o uso do Cloudinary (opcional): setar STORAGE_MODE=cloudinary
STORAGE_MODE=cloudinary

# Nota: mantenha segredos fora do repositório; use variáveis de ambiente no host/CI.
