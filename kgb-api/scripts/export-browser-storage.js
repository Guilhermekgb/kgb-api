/**
 * export-browser-storage.js (repo copy)
 *
 * Snippet para rodar no console do navegador.
 * Cria um arquivo JSON com `localStorage` + `sessionStorage` e opcionalmente
 * envia o dump para o endpoint `/api/storage-backup` usando header
 * `x-backup-token` (se o servidor usar `BACKUP_UPLOAD_TOKEN`).
 */

// Export-browser-storage moved to tools/legacy.
// Original utility relocated to `tools/legacy/export-browser-storage.kgb-api.legacy.js`.
// This file is kept as a lightweight placeholder to avoid counting legacy content
// in readiness metrics while not removing the original file path.
console.info('export-browser-storage: relocated to tools/legacy/export-browser-storage.kgb-api.legacy.js');
