# Filter: clientes

Matches: 35

First 50 entries:

- area-cliente.js:177 — const map = JSON.parse(localStorage.getItem('fotosClientes') || '{}');
- cadastro-cliente.html:514 — localStorage.setItem('clienteSelecionado', String(clienteId));
- cadastro-cliente.html:802 — localStorage.setItem('clienteSelecionado', String(novoCliente.id));
- cadastro-cliente.html:809 — localStorage.setItem('clienteSelecionado', String(novoCliente.id));
- cadastro-evento.js:70 — // ===== Fotos de clientes (armazenadas no localStorage) =====
- cadastro-evento.js:287 — const map = JSON.parse(localStorage.getItem("fotosClientes") || "{}");
- cadastro-evento.js:696 — localStorage.setItem('clienteSelecionado', String(clienteIdQS));
- cadastro-evento.js:703 — const idSelecionado = clienteIdQS || localStorage.getItem('clienteSelecionado');
- cadastro-evento.js:707 — const clientes = JSON.parse(localStorage.getItem('clientes') || '[]');
- cadastro-evento.js:823 — localStorage.removeItem('clienteSelecionado');
- cadastro-evento.js:949 — try { localStorage.removeItem('clienteSelecionado'); } catch {}
- cadastro-evento.js:994 — try { localStorage.removeItem('clienteSelecionado'); } catch {}
- cadastro-evento.js:1234 — const map = JSON.parse(localStorage.getItem('fotosClientes') || '{}');
- cadastro-evento.js:1258 — const clientes = JSON.parse(localStorage.getItem('clientes') || '[]');
- cadastro-evento.js:1437 — const clientes = JSON.parse(localStorage.getItem('clientes') || '[]');
- cadastro-evento.js:1707 — const clientes = JSON.parse(localStorage.getItem('clientes') || '[]');
- cliente-detalhado.html:734 — localStorage.setItem('clienteSelecionado', clienteSelecionado.id);
- cliente-detalhado.html:765 — const lista = JSON.parse(localStorage.getItem('clientes') || '[]');
- cliente-detalhado.html:768 — console.warn('[Cliente Detalhado] Erro ao ler clientes do localStorage:', e);
- clientes-lista.html:31 — try { localStorage.setItem("API_BASE", base); } catch(e) {}
- clientes-lista.js:41 — const raw = localStorage.getItem(k);
- clientes-lista.js:54 — for (let i = 0; i < localStorage.length; i++) {
- clientes-lista.js:55 — const key = localStorage.key(i);
- clientes-lista.js:60 — const raw = localStorage.getItem(key);
- clientes-lista.js:94 — const rawKey = localStorage.getItem(KEY_TIPOS_EVENTO);
- clientes-lista.js:104 — const raw = localStorage.getItem(k); if (!raw) continue;
- clientes-lista.js:180 — // 2) Procura no localStorage "eventos"
- clientes-lista.js:182 — const eventos = JSON.parse(localStorage.getItem('eventos') || '[]');
- contrato.js:9 — // === Limpa PDFs antigos guardados dentro de eventos/clientes (para evitar lotar localStorage) ===
- evento-detalhado.js:1662 — const map = JSON.parse(localStorage.getItem("fotosClientes") || "{}");
- evento-detalhado.js:1664 — localStorage.setItem("fotosClientes", JSON.stringify(map));
- evento-detalhado.js:1724 — const map = JSON.parse(localStorage.getItem("fotosClientes") || "{}");
- evento-detalhado.js:3427 — const map = JSON.parse(localStorage.getItem("fotosClientes") || "{}");
- evento-detalhado.js:3446 — localStorage.setItem("fotosClientes", JSON.stringify(map));
- lista-evento.html:337 — const map = JSON.parse(localStorage.getItem("fotosClientes") || "{}");