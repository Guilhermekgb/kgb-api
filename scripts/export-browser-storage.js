/**
 * export-browser-storage.js (repo copy)
 *
 * Snippet para rodar no console do navegador.
 * Cria um arquivo JSON com `localStorage` + `sessionStorage` e opcionalmente
 * envia o dump para o endpoint `/api/storage-backup` usando header
 * `x-backup-token` (se o servidor usar `BACKUP_UPLOAD_TOKEN`).
 */

// Export-browser-storage moved to tools/legacy.
// Original utility relocated to `tools/legacy/export-browser-storage.root.legacy.js` to avoid
// counting this helper in cloud-readiness metrics. The file remains here as a small
// placeholder so repository paths referencing it do not break.
// See tools/legacy/export-browser-storage.root.legacy.js for the full script.
console.info('export-browser-storage: relocated to tools/legacy/export-browser-storage.root.legacy.js');
